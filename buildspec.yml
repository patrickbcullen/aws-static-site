version: 0.2

phases:
  install:
    commands:
      - echo Installing packages...
      - npm install -g
  build:
    commands:
      - echo Build started on `date`
      - npm build
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - src/HelloWorld.js
